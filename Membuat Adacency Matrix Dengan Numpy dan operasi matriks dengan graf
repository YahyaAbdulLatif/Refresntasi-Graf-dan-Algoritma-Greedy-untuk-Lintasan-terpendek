#Membuat Adacency Matrix Dengan Numpy dan operasi matriks dengan graf
import numpy as np
nodes = ['A', 'B', 'C', 'D']
A = np.array([
    [0, 2, 4, 0],
    [2, 0, 0, 3],
    [4, 0, 0, 1],
    [0, 3, 1, 0]
])
print(A)
degree = np.sum(A > 0, axis=1)
print(degree)
